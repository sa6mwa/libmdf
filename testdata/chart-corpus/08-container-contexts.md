# Container Contexts

Blockquote horizontal chart.

> ```mdf-bar-chart
> key,value
> Quote Build,40
> Quote Test,25
> Quote Ship,10
> ```

Blockquote vertical chart.

> ```mdf-vertical-bar-chart
> key,value
> Quote Mon,4
> Quote Tue,7
> Quote Wed,3
> ```

Blockquote tile chart.

> ```mdf-tile-chart
> key,value
> Quote Build,40
> Quote Test,25
> Quote Ship,10
> ```

Unordered-list horizontal chart.

- Charts in a list:

  ```mdf-bar-chart
  key,value
  List Build,40
  List Test,25
  List Ship,10
  ```

Unordered-list vertical chart.

- Charts in a list:

  ```mdf-vertical-bar-chart
  key,value
  List Mon,4
  List Tue,7
  List Wed,3
  ```

Unordered-list tile chart.

- Charts in a list:

  ```mdf-tile-chart
  key,value
  List Build,40
  List Test,25
  List Ship,10
  ```

Ordered-list horizontal chart.

1. Charts in an ordered list:

   ```mdf-bar-chart
   key,value
   Ordered Build,40
   Ordered Test,25
   Ordered Ship,10
   ```

Ordered-list vertical chart.

1. Charts in an ordered list:

   ```mdf-vertical-bar-chart
   key,value
   Ordered Mon,4
   Ordered Tue,7
   Ordered Wed,3
   ```

Ordered-list tile chart.

1. Charts in an ordered list:

   ```mdf-tile-chart
   key,value
   Ordered Build,40
   Ordered Test,25
   Ordered Ship,10
   ```

Blockquoted-list horizontal chart.

> - Charts in a quoted list:
>
>   ```mdf-bar-chart
>   key,value
>   Quoted List Build,40
>   Quoted List Test,25
>   Quoted List Ship,10
>   ```

Blockquoted-list vertical chart.

> - Charts in a quoted list:
>
>   ```mdf-vertical-bar-chart
>   key,value
>   Quoted List Mon,4
>   Quoted List Tue,7
>   Quoted List Wed,3
>   ```

Blockquoted-list tile chart.

> - Charts in a quoted list:
>
>   ```mdf-tile-chart
>   key,value
>   Quoted List Build,40
>   Quoted List Test,25
>   Quoted List Ship,10
>   ```
